// This is our first server-side JavaScript code!
// console.log() is used to print messages to the terminal (or console).
console.log("Hello Class");

// In a web browser, we have access to the 'document' object which represents the HTML page.
// However, on the server side (like in Node.js), the 'document' variable is NOT defined
// because there is no web page to interact with.
// If you uncomment the line below and run this file using Node.js, you will get an error!
// console.log(document);
